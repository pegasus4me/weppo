# `@repo/eslint-config`

Shared ESLint configuration for workspace packages.

This package is internal to the monorepo and is consumed through the package
workspace rather than published independently.
